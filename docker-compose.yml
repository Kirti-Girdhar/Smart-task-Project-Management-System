services:

  web:

    build: .

    container_name: task_manager_container

    command: python manage.py runserver 0.0.0.0:8000

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - .:/app